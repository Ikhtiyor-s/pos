{
  "name": "oshxona-pos",
  "version": "1.0.0",
  "private": true,
  "description": "To'liq avtomatlashtirilgan Oshxona POS Tizimi",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "clean": "turbo run clean && rimraf node_modules",
    "db:generate": "turbo run db:generate --filter=@oshxona/database",
    "db:push": "turbo run db:push --filter=@oshxona/database",
    "db:migrate": "turbo run db:migrate --filter=@oshxona/database",
    "db:seed": "turbo run db:seed --filter=@oshxona/database",
    "db:studio": "turbo run db:studio --filter=@oshxona/database",
    "build:exe:pos": "cd apps/pos && npm run electron:build",
    "build:exe:kitchen": "cd apps/kitchen && npm run electron:build",
    "build:exe:waiter": "cd apps/waiter && npm run electron:build",
    "build:exe:web": "cd apps/web && npm run electron:build",
    "build:exe:all": "npm run build:exe:pos && npm run build:exe:kitchen && npm run build:exe:waiter && npm run build:exe:web",
    "build:apk:pos": "cd apps/pos && npm run cap:build",
    "build:apk:kitchen": "cd apps/kitchen && npm run cap:build",
    "build:apk:waiter": "cd apps/waiter && npm run cap:build",
    "build:apk:web": "cd apps/web && npm run cap:build"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "rimraf": "^5.0.5",
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "packageManager": "npm@10.2.0"
}
